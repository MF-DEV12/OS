SELECT notify, COUNT(*) total FROM( SELECT SupplyRequestNo id, DeliveredDate `date`, CustomerName `from`, 'Purchase Order(s)' `notify` FROM `vw_requestlistfromadmin` WHERE DeliveredStatus = 0 AND SupplierNo = #supno UNION SELECT RequestListNo id, DateReceive `date`, SupplierName `from`, 'Back Order(s)' `notify` FROM `vw_getbackorders` WHERE SupplierNo = #supno) tbl GROUP BY notify;