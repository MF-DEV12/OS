SELECT notify, COUNT(*) total, link FROM( SELECT SupplyRequestNo id, DeliveredDate `date`, CustomerName `from`, 'Request Order(s)' `notify`, 'requestlist' `link` FROM `vw_requestlistfromadmin` WHERE DeliveredStatus = 0 AND SupplierNo = #supno UNION SELECT RequestListNo id, DateReceive `date`, SupplierName `from`, 'Pending Order(s)' `notify`, 'backorders' `link` FROM `vw_getbackorders` WHERE SupplierNo = #supno) tbl GROUP BY notify;