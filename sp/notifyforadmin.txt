SELECT notify, COUNT(*) total, link FROM(SELECT SupplyRequestNo id, DeliveredDate `date`, SupplierName `from`, 'Received Order(s)' `notify`, 'receivings' `link` FROM `vw_getpotoreceive` UNION SELECT VariantNo id, '' `date`, '' `from`, 'New items(s)' `notify`, 'items' `link` FROM itemvariant WHERE Owned = 1 AND Price IS NULL AND LowStock IS NULL AND Critical IS NULL UNION SELECT VariantNo id, '' `date`, '' `from`, 'Low stock items(s)' `notify`, 'lowstocks' `link` FROM itemvariant WHERE STOCKS IS NOT NULL AND Stocks <= LowStock AND Stocks > Critical UNION SELECT VariantNo id, '' `date`, '' `from`, 'Critical stock items(s)' `notify`, 'lowstocks' `link` FROM itemvariant WHERE STOCKS IS NOT NULL AND Stocks <= Critical) tbl GROUP BY notify ; 